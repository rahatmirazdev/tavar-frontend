/**
 * App.jsx is currently unused. The actual router setup is in main.jsx
 * which uses RouterProvider from React Router to render the routes
 * defined in routes/root.jsx
 * 
 * This file is kept for future use if additional app-level
 * wrappers or providers are needed.
 */

function App() {
  // Note: Routing is handled by main.jsx with RouterProvider
  // This component is not currently used in the application
  return null;
}

export default App;
